import styles from "./Format.module.css";
// import { useAPI } from "../../context/apiContext";

export default function Format() {
  //   const { data, isLoading } = useAPI();

  //   const filteredData =
  //     points == 0
  //       ? data?.filter(
  //           (entry) => entry.army.indexOf(id.replaceAll("-", " ")) !== -1
  //         )
  //       : data?.filter(
  //           (entry) =>
  //             entry.army.indexOf(id.replaceAll("-", " ")) !== -1 &&
  //             entry.format == points
  //         );

  return (
    <>
      {/* <h2>Format</h2>
      <button className="btn btn-primary">2000</button>
      <button className="btn btn-primary">1500</button>
      <button className="btn btn-primary">Other</button> */}
    </>
  );
}
